#!/bin/env bash

VSCODE_PATH=$(command -v code)
NANO_PATH=$(command -v code)
VI_PATH=$(command -v code)

if [[ $VSCODE_PATH != "" ]]; then
    code --wait $1
elif [[ $NANO_PATH != "" ]]; then
    nano $1
else
    vi $1
fi